
#desarrollo 1#


class Libro:

 def __init__(self, titulo, autor, copias):
        self.titulo = titulo
        self.autor = autor
        self.copias = int (copias)

def consultar(self):
        print(f"{self.titulo}),{self.autor}. Numero de copias disponibles: {self.copias}.")


class Biblioteca:
    def __init__(self):
                self.listado = []

def agregarLibro(self, libro: libro):
    print(f"Felicidades, agregaste el libro {Libro.titulo} a tu biblioteca") 

def prestarLibro(self, libro:Libro): 
       for Libro in self.listado:
         if Libro.copias >= 1: 
            Libro.copias -1
            print(f"has retirado{self.titulo}")
         else: 
             print("no existen copias disponibles de prestamo")    